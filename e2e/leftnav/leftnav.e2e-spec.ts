import {$, $$, browser, ElementFinder, protractor} from 'protractor';

describe('leftnav', () => {
  const marginLeftOpen = '0';
  const marginLeftClosed = '-259px';

  beforeAll(() => {
    browser.get('/');
  });

  it('should be open for ')



})
